{
  "mcpServers": {
    "shopify-dev-mcp": {
      "command": "npx",
      "args": ["-y", "@shopify/dev-mcp@latest"],
      "description": "Official Shopify Dev MCP — search docs, explore API schemas, get development guidance."
    },
    "shopify-store-mcp": {
      "command": "node",
      "args": ["mcp-server/index.js"],
      "env": {
        "SHOPIFY_STORE_DOMAIN": "${SHOPIFY_STORE_DOMAIN}",
        "SHOPIFY_ACCESS_TOKEN": "${SHOPIFY_ACCESS_TOKEN}"
      },
      "description": "Custom MCP server for store-specific operations (products, orders, customers)."
    }
  }
}
